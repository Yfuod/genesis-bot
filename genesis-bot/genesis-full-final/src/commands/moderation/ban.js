import {SlashCommandBuilder,PermissionFlagsBits} from 'discord.js';export default{data:new SlashCommandBuilder().setName('ban').setDescription('ban').addUserOption(o=>o.setName('u').setDescription('u').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.BanMembers),async execute(i){const u=i.options.getUser('u');const m=await i.guild.members.fetch(u.id);await m.ban();i.reply('banned')}};
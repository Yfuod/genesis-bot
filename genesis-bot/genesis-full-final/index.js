import fs from 'fs';import path from 'path';import {Client,GatewayIntentBits,Collection} from 'discord.js';import config from './config.json' assert {type:'json'};const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildMembers,GatewayIntentBits.GuildMessages]});client.commands=new Collection();const cpath=path.join(process.cwd(),'src/commands');for(const folder of fs.readdirSync(cpath)){for(const file of fs.readdirSync(cpath+'/'+folder).filter(f=>f.endsWith('.js'))){const cmd=(await import(`./src/commands/${folder}/${file}`)).default;client.commands.set(cmd.data.name,cmd);}}const epath=path.join(process.cwd(),'src/events');for(const file of fs.readdirSync(epath).filter(f=>f.endsWith('.js'))){const evt=(await import(`./src/events/${file}`)).default;if(evt.once)client.once(evt.name,(...a)=>evt.execute(...a,client));else client.on(evt.name,(...a)=>evt.execute(...a,client));}client.login(config.token);